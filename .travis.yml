env:
  global:
  - REVIEWDOG_VERSION="0.9.10"

language: node_js
node_js: lts/*

script:
- npm run lint
- npm run build

after_failure:
- if [ "$TRAVIS_PULL_REQUEST" != "false" ]; then bash ./travis/reviewdog; fi
